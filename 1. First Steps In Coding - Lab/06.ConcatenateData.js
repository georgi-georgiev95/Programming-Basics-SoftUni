function solve([arg1, arg2, arg3, arg4]) {
    let name = arg1;
    let surname = arg2;
    let age = Number(arg3);
    let city = arg4;
    console.log(`You are ${name} ${surname}, a ${age}-years old person from ${city}.`);
}